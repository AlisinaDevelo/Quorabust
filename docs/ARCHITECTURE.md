# Architecture

Quorabust separates **training** (offline, reproducible, lineage-tracked) from
**serving** (a stateless FastAPI app), with a versioned **artifact contract** in
between. The diagrams below are the fastest way to understand the system; prose
follows.

## Component overview

```mermaid
flowchart LR
  subgraph offline["Offline · training"]
    raw[("raw CSV<br/>question pairs")]
    train["quorabust-train (CLI)"]
    artifact[("model artifact .pkl<br/>builder + clf + meta")]
    registry[("model registry<br/>models.jsonl")]
    report["quorabust-report (CLI)"]
    card["model card<br/>.md / .json"]
  end
  subgraph online["Online · serving"]
    api["quorabust-serve<br/>(FastAPI)"]
    metrics["/metrics<br/>Prometheus"]
  end
  raw --> train --> artifact
  train --> registry
  artifact --> report --> card
  artifact --> api
  api --> metrics
```

## Training pipeline

```mermaid
flowchart TD
  A[raw question pairs] --> B["clean_text (normalize)"]
  B --> C{feature backend}
  C -->|tfidf| D[TF-IDF pair features]
  C -->|embedding| E[sentence-transformer features]
  D --> F["pairwise features<br/>cos · jaccard · len_ratio · abs_len_diff · len_sum"]
  E --> F
  F --> G[XGBoost classifier]
  G --> H{eval-fraction &gt; 0?}
  H -->|yes| I[holdout metrics]
  H -->|no| J[skip holdout]
  I --> K[persist artifact + metadata sidecar]
  J --> K
  K --> L[append record to models.jsonl]
```

## Train vs. serve

```mermaid
sequenceDiagram
  actor Dev
  participant Train as quorabust-train
  participant FS as artifact + registry
  participant API as quorabust-serve
  actor Client

  Dev->>Train: --csv pairs.csv --out model.pkl
  Train->>Train: clean_text → features → XGBoost
  Train->>FS: write model.pkl (+ meta sidecar)
  Train->>FS: append models.jsonl record
  Note over API,FS: serving loads one artifact at startup
  API->>FS: load model.pkl
  Client->>API: POST /predict {question1[], question2[]}
  API->>API: build features, score pairs
  API-->>Client: proba_duplicate[]
  Client->>API: GET /metadata
  API-->>Client: version · backend · metrics · lineage
```

## Artifact + registry contract

```mermaid
erDiagram
  ARTIFACT ||--|| METADATA : embeds
  ARTIFACT ||--o{ REGISTRY_RECORD : "logged as"
  ARTIFACT {
    object builder "feature pipeline"
    object clf "XGBoost model"
    object meta "lineage + metrics"
  }
  METADATA {
    string feature_backend
    list   feature_schema
    int    n_train
    int    n_eval
    int    seed
    string git_revision
    string csv_sha256
    object metrics
  }
  REGISTRY_RECORD {
    string artifact_label
    string registered_at
    object metrics
  }
```

## Why it's shaped this way

- **Reproducible training.** Every artifact embeds its lineage — feature backend,
  feature schema, training size, seed, source-CSV checksum, and git revision — so a
  reviewer can tell exactly what produced a model without loading the pickle (a JSON
  sidecar carries the same metadata). See [REPORTING.md](REPORTING.md).
- **Stateless serving.** `quorabust-serve` loads one artifact at startup and exposes
  `/predict`, a safe `/metadata` view (no local paths), `/health`, `/ready`, and
  `/metrics`. Nothing is written at request time, so it scales horizontally.
- **Honest evaluation.** Holdout metrics and threshold sweeps are generated by
  `quorabust-report` into a model card; the checked-in
  [SAMPLE_MODEL_CARD.md](SAMPLE_MODEL_CARD.md) is built from synthetic smoke data and is
  explicitly not a benchmark.

## Where to look next

- [REPORTING.md](REPORTING.md) — model cards, metrics, and reproducing real numbers.
- [SCALING.md](SCALING.md) — batching, caching, and serving under load.
- [ENTERPRISE.md](ENTERPRISE.md) — A/B routing, drift checks, and operational notes.
